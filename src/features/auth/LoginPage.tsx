import { PagePlaceholder } from '../../components/ui/PagePlaceholder'

export function LoginPage() {
  return (
    <PagePlaceholder
      eyebrow="Authentication"
      title="Log In"
      description="Login placeholder for guardian and admin users."
    />
  )
}
